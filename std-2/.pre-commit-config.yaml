# Codex pre-commit hook configuration
# 使用 Codex 进行代码审查

repos:
  - repo: local
    hooks:
      - id: codex-review
        name: Codex Code Review
        entry: python scripts/codex_hook.py
        language: system
        types: [python]
        stages: [pre-commit]
        pass_filenames: true